export default {
  // Estos son los idiomas que tu aplicación soportará
  locales: ['en', 'es', 'fi', 'sv'],
  
  // El idioma por defecto si no se detecta ninguno o falla la ruta
  defaultLocale: 'en'
};